# GKlib
A library of various helper routines and frameworks used by many of the lab's software

## Building GKlib

To build GKlib you can follow the instructions below:
```console
$ cmake -B ./build
$ cmake --build ./build
```

See the top-level [`CMakeLists.txt`](CMakeLists.txt) for more details on the available options
